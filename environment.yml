name: joint-improvement
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  # All Python dependencies installed via setup script:
  # 1. PyTorch with CUDA 12.6
  # 2. Project package with required dependencies (-e .[dev])
